const assert = require("assert");
const { sum } = require("../src/index");

module.exports = {
  "sum should work": () => {
    assert.strictEqual(sum(1, 1), 2);
  },
  "sum should work2": () => {
    assert.strictEqual(sum(1, 1), 2);
  },
  "sum should work3": () => {
    assert.strictEqual(sum(1, 1), 2);
  },
  "sum should work4": () => {
    assert.strictEqual(sum(1, 1), 2);
  },
  "sum should work5": () => {
    assert.strictEqual(sum(1, 1), 2);
  },
  "sum should work6": () => {
    assert.strictEqual(sum(1, 1), 2);
  },
  "sum should work7": () => {
    assert.strictEqual(sum(1, 1), 2);
  },
};

import { GluegunToolbox } from "gluegun";
import { run } from "../../src/commands/bespin";

jest.mock("gluegun");

describe("bespin", () => {
  let toolbox: GluegunToolbox;

  beforeEach(() => {});
});
